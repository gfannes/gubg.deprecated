#ifndef HEADER_rinle_view_Font_hpp_ALREADY_INCLUDED
#define HEADER_rinle_view_Font_hpp_ALREADY_INCLUDED

#include <cstddef>

namespace rinle { namespace view { namespace font {

	size_t computeSize(size_t height, size_t nr = 1);

} } }

#endif
